import { LoaderCircle, Play, RotateCcw } from 'lucide-react'

const fields = [
  ['airTemperature', 'Air temperature', '°C', 'number'],
  ['relativeHumidity', 'Relative humidity', '%', 'number'],
  ['airVelocity', 'Air velocity', 'm/s', 'number'],
  ['meanRadiantTemperature', 'Mean radiant temperature', '°C', 'number'],
  ['metabolicRate', 'Metabolic rate', 'met', 'number'],
  ['clothingLevel', 'Clothing level', 'clo', 'number'],
  ['occupancy', 'Occupancy', '%', 'number'],
]

export default function ThermalForm({ values, errors, onChange, onReset, onSubmit, isLoading }) {
  return <form onSubmit={onSubmit} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm shadow-slate-200/40"><div className="grid gap-5 sm:grid-cols-2">{fields.map(([name, label, unit, type]) => <label key={name} className="block"><span className="text-xs font-bold text-slate-600">{label}</span><div className="relative mt-2"><input required type={type} step="0.1" name={name} value={values[name]} onChange={onChange} aria-invalid={Boolean(errors[name])} className={`w-full rounded-xl border bg-slate-50 px-3 py-2.5 pr-12 text-sm font-semibold text-slate-900 outline-none transition focus:border-orange-400 focus:ring-4 focus:ring-orange-100 ${errors[name] ? 'border-rose-400' : 'border-slate-200'}`} /><span className="pointer-events-none absolute inset-y-0 right-3 flex items-center text-xs font-medium text-slate-400">{unit}</span></div>{errors[name] && <span className="mt-1 block text-[11px] font-semibold text-rose-600">{errors[name]}</span>}</label>)}<label className="block"><span className="text-xs font-bold text-slate-600">Shelter type</span><select name="shelterType" value={values.shelterType} onChange={onChange} aria-invalid={Boolean(errors.shelterType)} className={`mt-2 w-full rounded-xl border bg-slate-50 px-3 py-2.5 text-sm font-semibold text-slate-900 outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100 ${errors.shelterType ? 'border-rose-400' : 'border-slate-200'}`}><option value="">Select shelter type</option><option>Bamboo and thatch</option><option>Corrugated tin</option><option>Concrete room</option><option>Cool roof shelter</option></select>{errors.shelterType && <span className="mt-1 block text-[11px] font-semibold text-rose-600">{errors.shelterType}</span>}</label><label className="block"><span className="text-xs font-bold text-slate-600">Location / region</span><input required name="location" value={values.location} onChange={onChange} aria-invalid={Boolean(errors.location)} className={`mt-2 w-full rounded-xl border bg-slate-50 px-3 py-2.5 text-sm font-semibold text-slate-900 outline-none focus:border-orange-400 focus:ring-4 focus:ring-orange-100 ${errors.location ? 'border-rose-400' : 'border-slate-200'}`} />{errors.location && <span className="mt-1 block text-[11px] font-semibold text-rose-600">{errors.location}</span>}</label></div><div className="mt-6 flex flex-col gap-3 sm:flex-row-reverse"><button disabled={isLoading} className="flex flex-1 items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-bold text-white transition hover:bg-orange-600 disabled:cursor-wait disabled:opacity-70"><span>{isLoading ? 'Calculating conditions...' : 'Run thermal analysis'}</span>{isLoading ? <LoaderCircle size={17} className="animate-spin" /> : <Play size={16} fill="currentColor" />}</button><button type="button" onClick={onReset} disabled={isLoading} className="flex items-center justify-center gap-2 rounded-xl border border-slate-200 px-4 py-3 text-sm font-bold text-slate-600 transition hover:border-slate-300 hover:bg-slate-50 disabled:opacity-50"><RotateCcw size={16} />Reset</button></div></form>
}
