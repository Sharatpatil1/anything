import { AlertTriangle, CheckCircle2, Lightbulb } from 'lucide-react'
import StatusBadge from './StatusBadge'

export default function ResultCard({ result }) {
  if (!result) return <div className="grid min-h-[300px] place-items-center rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-8 text-center"><div><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-white text-slate-300 shadow-sm"><Lightbulb size={22} /></div><p className="mt-4 text-sm font-bold text-slate-700">Your analysis result will appear here</p><p className="mt-1 text-xs text-slate-400">Adjust the shelter conditions and run a calculation.</p></div></div>
  const isAlert = ['Elevated', 'High', 'High heat stress'].includes(result.heatRisk)
  return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm shadow-slate-200/40"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-slate-400">Analysis result</p><div className="mt-3 flex items-baseline gap-2"><span className="text-4xl font-bold tracking-tight text-slate-900">{result.comfortIndex}</span><span className="text-sm font-medium text-slate-400">/ 100 comfort index</span></div></div><StatusBadge tone={isAlert ? 'warning' : 'success'}>{result.comfortCategory}</StatusBadge></div><div className="mt-6 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-400">Temperature</p><p className="mt-1 text-sm font-bold text-slate-800">{result.temperature}°C</p></div><div className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-400">Humidity</p><p className="mt-1 text-sm font-bold text-slate-800">{result.humidity}%</p></div></div><div className={`mt-4 flex items-center gap-2 rounded-xl p-3 text-xs font-bold ${isAlert ? 'bg-amber-50 text-amber-700' : 'bg-emerald-50 text-emerald-700'}`}>{isAlert ? <AlertTriangle size={16} /> : <CheckCircle2 size={16} />}Risk status: {result.heatRisk}</div><div className="mt-5"><p className="text-xs font-bold uppercase tracking-[0.16em] text-slate-400">Recommendations</p><ul className="mt-3 space-y-2">{result.recommendations.map((recommendation) => <li key={recommendation} className="flex gap-2 text-sm text-slate-600"><span className="mt-1.5 h-1.5 w-1.5 shrink-0 rounded-full bg-orange-500" />{recommendation}</li>)}</ul></div></div>
}
